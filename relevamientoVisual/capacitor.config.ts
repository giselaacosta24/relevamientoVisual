import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.relevamientoVisual.starter',
  appName: 'relevamientoVisual',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
